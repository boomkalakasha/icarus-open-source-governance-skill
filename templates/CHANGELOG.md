# Changelog

Use immutable SemVer tags. For each release record the exact commit/tag, user-visible changes, compatibility notes, migration/rollback instructions, release assets, checksums, and evidence boundaries. Do not call a local build or a GitHub Release a production deployment.
